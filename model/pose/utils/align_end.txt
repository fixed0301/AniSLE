
    # Save result (no display for headless server)
    cv2.imwrite(f"aligned_result_{idx}.jpg", result_img)
    print(f"결과가 'aligned_result_{idx}.jpg'로 저장되었습니다.")


if __name__ == "__main__":
    import argparse
    parser = argparse.ArgumentParser()
    parser.add_argument('--idx', type=str, required=True, help='Image index')
    args = parser.parse_args()
    align(args.idx)
